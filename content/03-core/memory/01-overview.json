{
    "title": "Memory 记忆系统",
    "path": ["核心子系统", "Memory 记忆系统", "概述"],
    "content": "<h1>Memory 记忆系统</h1><p>Hermes Agent 提供跨会话持久记忆，支持多种后端 provider。</p><h2>架构图</h2><pre class=\"mermaid\">graph TB\n    MEM[memory_tool.py]\n    MM[memory_manager.py]\n    MP[MemoryProvider ABC]\n    \n    subgraph \"Providers (插件)\"\n        HONCHO[honcho]\n        MEM0[mem0]\n        SUPER[supermemory]\n    end\n    \n    MEM --> MM\n    MM --> MP\n    MP --> HONCHO\n    MP --> MEM0\n    MP --> SUPER</pre><h2>MemoryProvider ABC</h2><pre><code class=\"language-python\">class MemoryProvider(ABC):\n    def sync_turn(self, user_id, role, content, **kwargs) -> None\n    def prefetch(self, user_id, query, **kwargs) -> list[dict]\n    def shutdown(self) -> None\n    def post_setup(self, hermes_home, config) -> None  # 可选</code></pre><h2>内置 Provider</h2><table><tr><th>Provider</th><th>类型</th><th>特点</th></tr><tr><td>honcho</td><td>本地</td><td>轻量，基于文件</td></tr><tr><td>mem0</td><td>云</td><td>AI 增强记忆</td></tr><tr><td>supermemory</td><td>云</td><td>链接一切</td></tr></table><h2>政策</h2><div class=\"warning-box\"><code>plugins/memory/</code> 目录是<strong>封闭的</strong>。新 provider 必须作为独立插件仓库发布。</div>"
}